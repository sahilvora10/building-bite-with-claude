# Session 03: v0.1 Build — "It works on my phone"

**Date:** 2026-04-04

## What was done
- Installed nvm + Node.js v22
- Scaffolded Expo app with TypeScript (blank template)
- Built SQLite database layer (`src/db/database.ts`)
  - meals table with full schema
  - getTodaysMeals + getTodaysTotals queries
- Built Gemini AI provider (`src/ai/gemini.ts`)
  - BYOM abstraction: `AIModelProvider` interface in `types.ts`
  - Gemini implementation with structured JSON output
  - System prompt tuned for Indian food / home-cooked meals
- Built home screen (`src/screens/HomeScreen.tsx`)
  - API key entry on first launch
  - Text input for meal logging
  - Today's totals card (calories, protein, carbs, fat)
  - Scrollable meal list with per-meal breakdown
- Verified: TypeScript compiles clean, Expo bundles for Android

## Files created
```
app/
├── App.tsx                    # Entry point, wires HomeScreen
├── src/
│   ├── ai/
│   │   ├── types.ts           # AIModelProvider interface (BYOM)
│   │   └── gemini.ts          # Gemini provider implementation
│   ├── db/
│   │   └── database.ts        # SQLite setup + meal queries
│   └── screens/
│       └── HomeScreen.tsx      # Main screen: input + totals + meal list
```

## Decisions made
- API key entered directly on home screen for v0.1 (settings screen in v0.4)
- Home screen IS the logging screen — zero navigation to log a meal
- Gemini `responseMimeType: 'application/json'` for structured output (no parsing hacks)

## What's next
- **User testing**: Install Expo Go on Android, scan QR, test the flow
- Get a Gemini API key from https://aistudio.google.com/apikey
- Log a real meal and verify the response
- Then proceed to v0.2 (photo input)

## Things necessary before next session
1. User needs Expo Go installed on Android phone
2. User needs a Gemini API key (free tier)
3. User tests the app and reports what works / what breaks
